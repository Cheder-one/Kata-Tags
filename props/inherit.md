## _Передать в дочерний элемент значение родительского_

Чтобы сделать ненаследумое свойство CSS - наследуемым, необходимо передать в дочерний элемент значение `inherit`:

```
.parent {
    padding-top: 10px;
}
.children {
    padding-top: inherit;
}
```
