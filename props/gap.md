### *Не влияет на расстояние до границ контейнера*
```
.grid-container {
  column-gap: 15px;
  row-gap: 5px;
}
```

### *Cоставное свойство `gap`:*
```
.grid-container {
  gap: 20px;
}
```

```
.grid-container {
  gap: 20px 50px;
		 %%row  col%%
}
```
